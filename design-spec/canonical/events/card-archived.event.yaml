kind: event
type: CardArchived
source: Card
description: 卡片聚合已从 suggested 进入 archived。
payload:
  fields:
    - id: cardId
      type: uuid
      required: true
    - id: reason
      type: text
      required: true
      description: 触发卡片归档的业务原因说明。
