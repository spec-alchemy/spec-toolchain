kind: command
type: archiveCard
target: Card
description: 归档建议卡片，并在卡片阶段提前结束闭环。
payload:
  fields:
    - id: cardId
      type: uuid
      required: true
    - id: reason
      type: text
      required: true
      description: 归档这张卡片的业务原因说明。
