kind: aggregate
objectId: Order
initial: draft
states:
  draft:
    on:
      submitOrder:
        target: submitted
        emit:
          type: OrderSubmitted
          payloadFrom:
            orderId: $command.orderId
            customerId: $command.customerId
            totalAmount: $command.totalAmount
  submitted: {}
